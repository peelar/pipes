---
'pipes': minor
---

Add `pipes upgrade` to self-update the installed binary from GitHub Releases, restarting the background server when one is running. `pipes upgrade --check` only reports availability.
